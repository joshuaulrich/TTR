1. Ability to handle and return other class structures (e.g. zoo, ts).

2. A plotting function that allows charting price, volume, indicators,
   etc. on one device (via quantmod?).

3. Functionality to summarize results of technical trading rules.
   (via PerformanceAnalytics?)
